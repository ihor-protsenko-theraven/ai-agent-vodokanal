# Server-side Gemini API key. Configure it only in the Vercel project settings.
GEMINI_API_KEY=

# `npm run dev` uses VITE_AI_MODE=local from .env.development by default.
# Set VITE_AI_MODE=gemini only when a local server is also serving /api/gemini.
